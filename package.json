{
  "name": "dwg-buscador-web",
  "version": "1.0.0",
  "private": true,
  "description": "Visor web de archivos DWG con búsqueda, zoom y marcador sobre el elemento encontrado.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check config.js && node --check services/aps.js && node --check routes/auth.js && node --check routes/models.js && node --check public/viewer.js && node --check public/search.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aps_sdk/authentication": "^1.0.0",
    "@aps_sdk/model-derivative": "^1.0.0",
    "@aps_sdk/oss": "^1.0.1",
    "dotenv": "^16.4.1",
    "express": "^4.21.2",
    "express-formidable": "^1.2.0"
  },
  "devDependencies": {
    "@types/forge-viewer": "^7.48.0"
  }
}
